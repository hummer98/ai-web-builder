# direnv 用テンプレート: cp .envrc.example .envrc して編集後 `direnv allow`
#
# このファイルにはサーバー側鍵（GitHub App 等）のみを書く。
# ユーザーの API 鍵 (OpenRouter / Gemini / Cloudflare / Firebase) は
# editor の設定画面から登録する (BYOK) — data/secrets.json (0600) に保存される。

# GitHub App
export GITHUB_APP_ID=
export GITHUB_APP_PRIVATE_KEY=
export GITHUB_OWNER=

# Cloudflare Access (本番認証、Fly.io 専用)
export CLOUDFLARE_ACCESS_AUD=
export CLOUDFLARE_ACCESS_TEAM_DOMAIN=
export ALLOWED_ORIGINS=

# Playwright Demo (テスト用)
export DEMO_PASSWORD=
export DEMO_URL=
